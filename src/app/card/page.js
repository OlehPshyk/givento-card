export default async function Postcards({params}) {
  return (
    <main>
      <span>it just for nginx testing(delete this page.js when done)</span>
      <h1 style={{height:'100vh',display:'flex',justifyContent:'center',alignItems:'center'}}>Postcards</h1>
    </main>
  )
}
